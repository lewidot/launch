{
	"name": "server",
	"version": "0.0.0",
	"devDependencies": {
		"@types/bun": "latest"
	},
	"scripts": {
		"dev": "bun run --hot src/index.ts",
		"build": "bun build --compile --minify --sourcemap ./src/index.ts --outfile server"
	},
	"type": "module"
}

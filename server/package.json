{
	"name": "server",
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "bun run --hot src/index.ts",
		"build": "bun build --compile --minify --sourcemap ./src/index.ts --outfile server"
	},
	"dependencies": {
		"hono": "^4.11.7"
	},
	"devDependencies": {
		"@types/bun": "latest"
	}
}

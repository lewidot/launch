{
	"name": "launch",
	"version": "0.0.0",
	"module": "src/index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "bun run --hot src/index.ts"
	},
	"dependencies": {
		"hono": "^4.11.7"
	},
	"devDependencies": {
		"bun-types": "latest",
		"prettier": "^3.8.1"
	}
}
